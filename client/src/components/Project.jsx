function Project(props){
return (
    <>
    <img src={props.imgs} alt={props.alts}>
        {/* <a href={props.repoLink}>
            <h2>{props.title}</h2>
        </a> */}
    </img>
    </>
)
}

export default Project;